services:
  forecast:
    build: .
    container_name: hulkfit-forecast
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8051:8051"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
